# CLAUDE.md — 만물마켓 작업 규칙

이 파일은 Claude Code가 이 폴더에서 일할 때 **항상 먼저 읽는 안내서**입니다.

---

## 0. 이 프로젝트는 뭔가요

- **이름**: 만물마켓 (manmul-market) — 동네 중고거래 연습 사이트
- **성격**: 실무 서비스가 아니라 **개발 연습용 학습 프로젝트**입니다.
- **사용자**: 비개발자. 코딩을 배우는 중입니다.

---

## 1. 대화 규칙 (제일 중요)

### 1-1. 쉬운 말로 설명하기
개발 용어가 나오면 **반드시 풀어서** 설명합니다. 용어를 그냥 던지지 않습니다.

- ❌ "서버 액션으로 처리하고 미들웨어에서 세션 리프레시할게요"
- ✅ "버튼을 누르면 **서버 쪽에서 실행되는 함수**(서버 액션)로 처리할게요. 비밀번호 같은 건 브라우저가 아니라 서버에서 다뤄야 안전하거든요."

자주 쓰는 용어는 이렇게 풀어 씁니다:

| 용어 | 쉬운 설명 |
| --- | --- |
| 컴포넌트 | 화면을 이루는 조각 (버튼, 카드 같은 것) |
| 서버 컴포넌트 | 서버에서 미리 만들어 보내주는 화면 |
| 클라이언트 컴포넌트 | 브라우저에서 움직이는 화면 (클릭, 입력 반응) |
| 서버 액션 | 폼을 제출하면 서버에서 실행되는 함수 |
| 미들웨어 | 모든 요청이 지나가는 검문소 |
| 마이그레이션 | DB 구조를 바꾸는 명령문을 파일로 남겨둔 것 |
| RLS | "이 줄은 누가 볼 수 있나"를 DB가 직접 지키는 보안 규칙 |
| 환경변수 | 비밀번호·주소 같은 걸 코드 밖에 따로 적어두는 쪽지 |
| 배포 | 내 컴퓨터가 아니라 인터넷에 올려서 남도 볼 수 있게 하는 것 |

### 1-2. 진행 상황을 자주 말하기
작업이 길어지면 **중간중간 지금 뭘 하는지** 한 줄씩 알려줍니다.

```
👉 지금: 상품 테이블(mm_products) 만드는 중
✅ 끝: 테이블 생성 완료
👉 다음: 상품 등록 화면 만들기
```

말없이 오래 작업하지 않습니다.

### 1-3. 시킨 것만, 단계적으로
- 사용자가 **요청한 단계만** 구현합니다.
- "이것도 같이 해두면 좋을 것 같아서" 하는 **선제 작업은 하지 않습니다.**
- 다음 단계가 떠오르면 **만들지 말고 제안만** 합니다.
  → "다음엔 찜하기 기능을 붙이면 좋겠어요. 할까요?"

### 1-4. 할 수 있는 건 최대한 직접 하기
Claude가 할 수 있는 일은 **묻지 말고 그냥 합니다.**
- 파일 만들기 / 고치기
- 패키지 설치 (`npm install`)
- DB 테이블 만들기 (Supabase MCP 도구 사용)
- 개발 서버 실행 & 화면 확인
- git 커밋 (사용자가 커밋해달라고 했을 때)

**사람이 꼭 해야 하는 일**만 따로 모아서 안내합니다. 이럴 땐 번호를 매기고,
"어느 사이트 → 어느 메뉴 → 뭘 누르면 되는지"까지 적어줍니다.

```
🙋 이건 직접 해주셔야 해요

1. https://supabase.com/dashboard 접속
2. 왼쪽 메뉴에서 Storage 클릭
3. "New bucket" 버튼 → 이름에 `mm-products` 입력 → Public 체크 → Save
4. 다 하시면 "됐어요" 라고만 알려주세요
```

### 1-5. 끝나면 바로 눈으로 확인시키기
화면과 관련된 작업을 마치면 **항상 브라우저를 열어서 보여줍니다.**
- `preview_start`로 개발 서버(`manmul-market`)를 띄우고
- 만든 화면으로 이동해서
- 스크린샷까지 보여준 뒤 작업 완료를 알립니다.

"직접 켜서 확인해보세요"라고 떠넘기지 않습니다.

---

## 2. 기술 스택

| 항목 | 내용 |
| --- | --- |
| 프레임워크 | Next.js 15 (App Router) + TypeScript |
| 스타일 | Tailwind CSS v4 (설정은 `src/app/globals.css`의 `@theme`) |
| 백엔드/DB | Supabase (PostgreSQL + Auth) |
| 배포 | Vercel |
| 실행 | `npm run dev` → http://localhost:3000 |

---

## 3. ⚠️ Supabase DB 공유 규칙 (꼭 지키기)

이 프로젝트는 **가계부 프로젝트와 같은 Supabase 데이터베이스**(`ggb0921_db`)를 씁니다.

- 만물마켓이 만드는 **테이블·함수·트리거·정책 이름은 전부 `mm_` 로 시작**합니다.
  - 예: `mm_profiles`, `mm_products`, `mm_likes`
- 가계부 테이블(`entries`, `settings`)은 **읽지도 고치지도 않습니다.**
- DB를 바꾸면 **반드시 `supabase/migrations/` 에 SQL 파일로 남깁니다.**
  - 파일명: `YYYYMMDD_순번_mm_설명.sql` (예: `20260921_001_mm_create_profiles.sql`)
- 새 테이블에는 **항상 RLS(접근 권한 규칙)를 켜고** 정책을 같이 만듭니다.

---

## 4. 코드 작성 규칙

- **주석은 한국어로 충분히** 답니다. 비개발자가 나중에 읽고 이해할 수 있을 정도로.
  - 이 함수가 뭘 하는지, 왜 이렇게 했는지를 적습니다.
- 파일·폴더 구조는 기존 방식을 그대로 따릅니다.
  - 페이지: `src/app/<경로>/page.tsx` (서버 컴포넌트)
  - 폼처럼 움직이는 부분: 같은 폴더에 `XxxForm.tsx` (클라이언트 컴포넌트)
  - 공용 조각: `src/components/`
  - 서버 액션·Supabase 연결: `src/lib/`
- 디자인 토큰(`carrot-500`, `leaf-500`, `ink`, `rounded-blob` 등)을 사용하고
  임의의 색상 코드를 새로 만들지 않습니다.
- 사용자에게 보이는 글자는 **전부 한국어**, 에러 메시지도 한국어로 풀어 씁니다.

---

## 5. 지금까지 진행 상황

- ✅ **1단계** — 회원가입 / 로그인 / 로그아웃 / 마이페이지 / `mm_profiles` 테이블
- ✅ **2단계** — 거래 글 CRUD (올리기 / 목록·검색 / 상세 / 수정 / 삭제 / 판매상태 변경),
  사진 업로드(`mm-products` 스토리지 버킷), `mm_products` 테이블
- ⬜ 3단계 이후 — 사용자가 요청할 때 진행

### 2단계에서 알게 된 것 (같은 실수 반복하지 않기)

- 서버 액션 폼에서 **눌린 버튼의 `name`/`value` 는 전달되지 않습니다.**
  버튼으로 값을 구분하려면 버튼마다 폼을 따로 만들고 `hidden` input 으로 값을 넣으세요.
  (`src/components/products/StatusSelect.tsx` 참고)
- `next dev` 가 켜져 있는 동안 `npm run build` 를 돌리면 `.next` 폴더가 섞여
  화면에서 CSS 가 사라집니다. 둘을 동시에 돌리지 마세요.

자세한 내용(폴더 구조, DB 컬럼, 테스트 계정, 디자인 토큰)은 `README.md` 참고.

---

## 6. 작업을 마칠 때 체크리스트

1. 코드에 한국어 주석이 충분한가
2. DB를 바꿨다면 `supabase/migrations/`에 SQL을 남겼는가
3. 개발 서버를 띄워 **화면을 직접 확인**했는가
4. 사람이 해야 할 일이 남았다면 **따로 정리해서 안내**했는가
5. 무엇을 만들었는지 **쉬운 말로 요약**했는가
